{
  "name": "node-starter",
  "version": "0.0.0",
  "scripts": {
    "start": "concurrently \"cd client && npm i && npm run dev\" \"cd server && npm i && npm run start:dev\""
  },
  "dependencies": {
    "@nestjs/cli": "^9.0.0",
    "concurrently": "^7.3.0"
  }
}
